import { ICellConfig } from '@armathai/grid-core';
export declare type IPixiChild = PIXI.Container & {
    destroy(...args: unknown[]): void;
    resize?(width: number, height: number): void;
    postBuild?(): void;
};
export declare type IPixiGrid = IPixiChild & {
    getGridConfig(): ICellConfig;
    rebuild(config?: ICellConfig): void;
};
export declare type IContent = IPixiChild | IPixiGrid;
declare global {
    interface Window {
        game: PIXI.Application;
    }
}
