import { Cell, IDebug } from '@armathai/grid-core';
import { IContent, IPixiGrid } from './Types';
export declare class Debug {
    private _debugger;
    private _parent;
    constructor(parent: IPixiGrid);
    bringToTop(): void;
    draw(cell: Cell<IContent>, lineWidth?: number, parentDebug?: IDebug): void;
    clear(): void;
    strokeRect(x: number, y: number, w: number, h: number, lineWidth: number, color?: number): void;
    fillRect(x: number, y: number, w: number, h: number, lineWidth: number, color?: number): void;
}
export declare class Debugger extends PIXI.Graphics {
    static readonly defaultColor: number;
    strokeRect(x: number, y: number, w: number, h: number, lineWidth: number, color?: number): void;
    fillRect(x: number, y: number, w: number, h: number, lineWidth: number, color?: number): void;
}
