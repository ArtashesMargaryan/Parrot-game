import { AbstractLego } from '../AbstractLego';
import { ICommand, IGuard } from '../Types';
import { AbstractCommand } from './AbstractCommand';
export declare class Command extends AbstractCommand {
    private _lego;
    private _guards;
    private _payloads;
    constructor(lego: AbstractLego);
    on(event: string, command: ICommand): this;
    once(event: string, command: ICommand): this;
    off(event: string, command: ICommand): this;
    execute(...commands: ICommand[]): this;
    payload(...args: unknown[]): this;
    guard(...args: IGuard[]): this;
    private _execute;
    private _resetGuardsAndPayloads;
    private _getEventCallback;
}
