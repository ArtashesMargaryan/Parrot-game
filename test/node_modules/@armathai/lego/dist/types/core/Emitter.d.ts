import { ICallback, IListener } from '../Types';
import { AbstractEmitter } from './AbstractEmitter';
export declare class Emitter extends AbstractEmitter {
    private _events;
    getListeners(event: string): IListener[];
    on(name: string, callback: ICallback, context?: unknown): this;
    once(name: string, callback: ICallback, context?: unknown): this;
    off(name: string, callback: ICallback, context?: unknown): this;
    emit(name: string, ...args: unknown[]): void;
    removeListenersOf(context: unknown): void;
    private _addListener;
    private _removeListener;
}
