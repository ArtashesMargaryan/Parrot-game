import { AbstractLego } from '../AbstractLego';
import { AbstractObserve } from './AbstractObserve';
export declare class Observe extends AbstractObserve {
    private _lego;
    constructor(lego: AbstractLego);
    makeObservable(obj: unknown & {
        uuid: unknown;
    }, ...props: string[]): void;
    removeObservable(obj: unknown, ...props: string[]): void;
    createObservable(obj: unknown & {
        uuid: unknown;
    }, prop: string, value: unknown): void;
}
