import { AbstractLego } from './AbstractLego';
import { AbstractCommand } from './core/AbstractCommand';
import { AbstractEmitter } from './core/AbstractEmitter';
import { AbstractObserve } from './core/AbstractObserve';
declare class Lego implements AbstractLego {
    private readonly _observe;
    private readonly _event;
    private readonly _command;
    constructor();
    not(fn: (...args: unknown[]) => boolean): (...args: unknown[]) => boolean;
    get observe(): AbstractObserve;
    get event(): AbstractEmitter;
    get command(): AbstractCommand;
}
export declare const lego: Lego;
export {};
