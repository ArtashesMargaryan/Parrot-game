export declare type IRawPoint = {
    x?: number;
    y?: number;
};
export declare type IRawRect = {
    x?: number;
    y?: number;
    width?: number;
    height?: number;
};
export declare type IRawBounds = {
    x?: number | string;
    y?: number | string;
    width?: number | string;
    height?: number | string;
};
export declare type IRawPadding = number | IRawRect;
export declare type IDebug = {
    color?: number;
    fill?: boolean;
};
export declare type ICellConfig = {
    name: string;
    debug?: IDebug;
    scale?: CellScale;
    align?: CellAlign;
    cells?: ICellConfig[];
    bounds?: IRawBounds;
    padding?: IRawPadding;
    offset?: IRawPoint;
};
export declare type IDimension = {
    width: number;
    height: number;
};
export declare enum CellScale {
    none = 1,
    fit = 2,
    fill = 3,
    showAll = 4,
    envelop = 5,
    custom = 6
}
export declare enum CellAlign {
    none = 1,
    center = 2,
    centerTop = 3,
    centerBottom = 4,
    leftCenter = 5,
    leftTop = 6,
    leftBottom = 7,
    rightCenter = 8,
    rightTop = 9,
    rightBottom = 10
}
